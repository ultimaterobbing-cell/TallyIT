<form method="POST" action="{{ $action }}" class="space-y-8">
    @csrf
    @if ($method !== 'POST') @method($method) @endif
    <div><label for="task_name" class="mb-2 block text-xs font-bold uppercase tracking-[0.16em] text-ink/60">Task name</label><input id="task_name" name="task_name" value="{{ old('task_name', $task->task_name ?? '') }}" required maxlength="120" placeholder="e.g. Prepare project presentation" class="w-full border-0 border-b-2 border-ink/20 bg-transparent px-0 py-3 text-xl font-semibold outline-none transition placeholder:text-ink/25 focus:border-coral">@error('task_name')<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror</div>
    <div><label for="description" class="mb-2 block text-xs font-bold uppercase tracking-[0.16em] text-ink/60">Description <span class="font-normal normal-case tracking-normal text-ink/40">(optional)</span></label><textarea id="description" name="description" rows="4" maxlength="1000" placeholder="What does done look like?" class="w-full resize-none border border-ink/15 bg-paper px-4 py-3 leading-6 outline-none transition focus:border-coral">{{ old('description', $task->description ?? '') }}</textarea></div>
    <div class="grid gap-8 sm:grid-cols-2"><div><label for="due_date" class="mb-2 block text-xs font-bold uppercase tracking-[0.16em] text-ink/60">Due date <span class="font-normal normal-case tracking-normal text-ink/40">(optional)</span></label><input id="due_date" type="date" name="due_date" value="{{ old('due_date', isset($task) && $task->due_date ? $task->due_date->format('Y-m-d') : '') }}" class="w-full border-0 border-b-2 border-ink/20 bg-transparent px-0 py-3 outline-none transition focus:border-coral"></div><div><label for="status" class="mb-2 block text-xs font-bold uppercase tracking-[0.16em] text-ink/60">Status</label><select id="status" name="status" class="w-full border-0 border-b-2 border-ink/20 bg-transparent px-0 py-3 outline-none transition focus:border-coral"><option value="Pending" @selected(old('status', $task->status ?? 'Pending') === 'Pending')>Pending</option><option value="Completed" @selected(old('status', $task->status ?? '') === 'Completed')>Completed</option></select></div></div>
    <div class="flex flex-wrap items-center justify-end gap-5 border-t border-ink/15 pt-6"><a href="{{ route('tasks.index') }}" class="text-sm font-semibold text-ink/55 underline decoration-ink/20 underline-offset-4 hover:text-ink">Cancel</a><button class="border-b-2 border-coral px-1 py-3 text-sm font-bold transition hover:text-coral">{{ $button }} <span class="ml-2">→</span></button></div>
</form>
