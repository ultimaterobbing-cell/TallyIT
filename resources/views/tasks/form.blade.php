<form method="POST" action="{{ $action }}" class="space-y-6">
    @csrf
    @if ($method !== 'POST') @method($method) @endif
    <div><label for="task_name" class="mb-2 block text-sm font-semibold">Task name</label><input id="task_name" name="task_name" value="{{ old('task_name', $task->task_name ?? '') }}" required maxlength="120" placeholder="e.g. Prepare project presentation" class="w-full rounded-xl border border-ink/15 bg-paper px-4 py-3 outline-none transition focus:border-coral focus:ring-4 focus:ring-coral/10">@error('task_name')<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror</div>
    <div><label for="description" class="mb-2 block text-sm font-semibold">Description <span class="font-normal text-ink/40">(optional)</span></label><textarea id="description" name="description" rows="4" maxlength="1000" placeholder="What does done look like?" class="w-full resize-none rounded-xl border border-ink/15 bg-paper px-4 py-3 outline-none transition focus:border-coral focus:ring-4 focus:ring-coral/10">{{ old('description', $task->description ?? '') }}</textarea></div>
    <div class="grid gap-5 sm:grid-cols-2"><div><label for="due_date" class="mb-2 block text-sm font-semibold">Due date <span class="font-normal text-ink/40">(optional)</span></label><input id="due_date" type="date" name="due_date" value="{{ old('due_date', isset($task) && $task->due_date ? $task->due_date->format('Y-m-d') : '') }}" class="w-full rounded-xl border border-ink/15 bg-paper px-4 py-3 outline-none transition focus:border-coral focus:ring-4 focus:ring-coral/10"></div><div><label for="status" class="mb-2 block text-sm font-semibold">Status</label><select id="status" name="status" class="w-full rounded-xl border border-ink/15 bg-paper px-4 py-3 outline-none transition focus:border-coral focus:ring-4 focus:ring-coral/10"><option value="Pending" @selected(old('status', $task->status ?? 'Pending') === 'Pending')>Pending</option><option value="Completed" @selected(old('status', $task->status ?? '') === 'Completed')>Completed</option></select></div></div>
    <div class="flex items-center justify-end gap-3 border-t border-ink/10 pt-6"><a href="{{ route('tasks.index') }}" class="rounded-xl px-4 py-3 text-sm font-semibold text-ink/55 hover:text-ink">Cancel</a><button class="rounded-xl bg-ink px-5 py-3 text-sm font-semibold text-white transition hover:bg-coral">{{ $button }} <span class="ml-2">→</span></button></div>
</form>
